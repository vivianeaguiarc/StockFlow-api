Você é um Tech Lead Backend Senior especialista em Node.js, TypeScript, Express, Prisma ORM, PostgreSQL, Redis e deploy no Render.

Estou construindo a StockFlow API e quero preparar o projeto para deploy real no Render.

Objetivo:
Ajustar scripts, variáveis e documentação para publicar a API no Render usando PostgreSQL e Redis gerenciados.

Tarefas:

1. Verificar package.json.
2. Garantir script build.
3. Garantir script start.
4. Garantir script db:migrate:deploy.
5. Garantir que o server use process.env.PORT.
6. Garantir que Prisma use DATABASE_URL.
7. Atualizar .env.example com variáveis de produção.
8. Atualizar README com passo a passo de deploy no Render.
9. Criar docs/render-deploy.md.
10. Não commitar .env.
11. Não expor segredos reais.

Scripts esperados:
"build": "tsc"
"start": "node dist/server.js"
"db:migrate:deploy": "prisma migrate deploy"
"db:generate": "prisma generate"

Variáveis:
NODE_ENV=production
PORT=3333
DATABASE_URL=
REDIS_URL=
JWT_SECRET=
JWT_EXPIRES_IN=
REFRESH_TOKEN_EXPIRES_IN=
CACHE_TTL_SECONDS=

Configuração Render:
Build Command:
pnpm install --frozen-lockfile && pnpm db:generate && pnpm build

Pre-Deploy Command:
pnpm db:migrate:deploy

Start Command:
pnpm start

Health Check Path:
/api/v1/health/ready

Commit esperado:
docs: add render deployment guide
